name: Docker Image CI

on: [push]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Build the Docker image
      run: docker build . --file at-image/Dockerfile --tag  sviatstetsiak/auto-test:jdk11-dotnet2.2-nodejs8-$(date +%s)
